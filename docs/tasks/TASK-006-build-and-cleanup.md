# TASK-006 — Build Verification & Final Cleanup

## Goal
Ensure the production build is clean and the codebase is tidy after all
previous tasks.

## Acceptance Criteria
- [ ] `npm run build` exits 0 with no TypeScript errors
- [ ] No `console.error` / `console.warn` noise in the test run
- [ ] `DeviceConfigPage` has at least 5 smoke tests
- [ ] `MinimizedViews` component has at least 3 smoke tests
- [ ] README documents how to run dev, test, and build

## Status
DONE
